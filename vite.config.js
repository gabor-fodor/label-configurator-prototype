import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  // Set the base path to your repository name
  // Example: base: '/label-configurator-prototype/'
  // For relative paths (works in most cases):
  base: './', 
})
